{
  "name": "four-meme-fork-infra",
  "version": "0.1.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run --network hardhat scripts/deploy.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^4.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.14.0",
    "@openzeppelin/contracts": "^4.9.0",
    "chai": "^4.3.7"
  }
}
